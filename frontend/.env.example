# Public, build-time frontend configuration. Never put secrets in VITE_ variables.

# Leave blank when the frontend and backend share one public origin.
VITE_API_BASE_URL=

# Used only by the local Vite development proxy.
VITE_API_PROXY_TARGET=http://localhost:8080
